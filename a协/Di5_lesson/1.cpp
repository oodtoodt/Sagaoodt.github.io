/*
  Id:
  LANG:C++
  problem:迷宫城堡
*/
#include <iostream>
#include <algorithm>
#include <cstring>
#include <cstdio>
#include <map>

using namespace std;

const int maxx = 1e6;


int main()
{
#ifdef LOCAL
    freopen("/Users/ecooodt/Desktop/c++ and acm/a协/Di5_lesson/1.txt","r",stdin);
#endif
    int n,m;
    while(scanf("%d%d",&n,&m) && (n != 0 && m != 0))
    {
        for(int i = 0; i < m; i++)
        {
            int a,b;
            scanf("%d%d",&a,&b);
        }

    }
    return 0;
}